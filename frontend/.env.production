# Production builds are served behind nginx on the same host.
# Leave empty so /api and /ws use same-origin (not localhost).
VITE_API_BASE=
VITE_PAYLOAD_OBFUSCATION_KEY=AcahNgPKCEjDgkF3GHjksPjQOu2URHoo9FqntVMDf-E
